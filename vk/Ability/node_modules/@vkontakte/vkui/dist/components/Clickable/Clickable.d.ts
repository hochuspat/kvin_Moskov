import * as React from 'react';
import { FocusVisibleMode } from '../FocusVisible/FocusVisible';
import { RootComponentProps } from '../RootComponent/RootComponent';
export interface ClickableProps<T> extends RootComponentProps<T> {
    baseClassName?: string;
    focusVisibleMode?: FocusVisibleMode;
}
/**
 * Базовый кликабельный корневой компонент.
 *
 * - при передаче `href` превратится в `a`,
 * - при передаче `onClick` превратится в `div` c `role="button"` и `tabIndex="0"`.
 * - иначе используется `div`.
 */
export declare const Clickable: <T>({ focusVisibleMode, ...restProps }: ClickableProps<T>) => React.JSX.Element;
//# sourceMappingURL=Clickable.d.ts.map